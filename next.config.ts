import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Pipeline work happens in API routes via child processes (ffmpeg); nothing special needed here.
};

export default nextConfig;
